# ==============================================
# NFT Multichain Minter Backend - Environment Configuration
# ==============================================

# --------------------------------------------
# Application
# --------------------------------------------
PORT=8000
NODE_ENV=development

# --------------------------------------------
# JWT Authentication (CRITICAL SECURITY)
# --------------------------------------------
# JWT secret for signing tokens
# REQUIRED: Minimum 32 characters (64+ recommended for production)
# Generate with: openssl rand -hex 32
JWT_SECRET=

# JWT expiry duration (default: 2h)
JWT_EXPIRES_IN=2h

# --------------------------------------------
# Redis Configuration
# --------------------------------------------
# Redis connection URL for nonce storage and rate limiting
REDIS_URL=redis://127.0.0.1:6379

# --------------------------------------------
# CORS Configuration
# --------------------------------------------
# Frontend URL for CORS (production only)
# Development uses localhost:3000
FRONTEND_URL=https://yourdomain.com

# --------------------------------------------
# IPFS / NFT.Storage
# --------------------------------------------
# API key from https://nft.storage
# REQUIRED for IPFS uploads
NFT_STORAGE_API_KEY=

# --------------------------------------------
# Ethereum Configuration
# --------------------------------------------
# Ethereum RPC provider URL (Infura, Alchemy, etc.)
ETH_RPC_URL=https://sepolia.infura.io/v3/YOUR_PROJECT_ID

# Deployed ERC-721 contract address
ETH_CONTRACT_ADDRESS=

# Contract deployer private key (for backend minting if needed)
# WARNING: Keep this secure! Never commit to version control
ETH_PRIVATE_KEY=

# Network (mainnet, sepolia, goerli, etc.)
ETH_NETWORK=sepolia

# --------------------------------------------
# Hedera Configuration
# --------------------------------------------
# Hedera network (mainnet or testnet)
HEDERA_NETWORK=testnet

# Hedera operator account ID (format: 0.0.xxxxx)
HEDERA_OPERATOR_ID=

# Hedera operator account private key
# WARNING: Keep this secure! Never commit to version control
HEDERA_OPERATOR_KEY=

# Hedera NFT Token ID (HTS token, format: 0.0.xxxxx)
HEDERA_NFT_TOKEN_ID=

# --------------------------------------------
# Rate Limiting Configuration (Optional)
# --------------------------------------------
# Rate limits per wallet per minute
ETH_RATE_LIMIT_PER_WALLET=10
HEDERA_RATE_LIMIT_PER_WALLET=5

# Global API rate limit (requests per 15 minutes)
GLOBAL_RATE_LIMIT=100

# --------------------------------------------
# Circuit Breaker Configuration (Optional)
# --------------------------------------------
# Ethereum RPC circuit breaker
ETH_RPC_TIMEOUT=8000
ETH_RPC_ERROR_THRESHOLD=50
ETH_RPC_RESET_TIMEOUT=15000

# Hedera SDK circuit breaker
HEDERA_TIMEOUT=10000
HEDERA_ERROR_THRESHOLD=40
HEDERA_RESET_TIMEOUT=20000

# IPFS circuit breaker
IPFS_TIMEOUT=10000
IPFS_ERROR_THRESHOLD=50
IPFS_RESET_TIMEOUT=30000
